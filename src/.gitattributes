
# Normalizar todos los archivos de texto a LF en el repositorio
* text=auto eol=lf

# Archivos de código (HTML, CSS, JS, etc.) siempre con LF
*.html text eol=lf
*.css  text eol=lf
*.js   text eol=lf
*.md   text eol=lf

# Archivos especiales de Windows que sí necesitan CRLF
*.bat  text eol=crlf
*.cmd  text eol=crlf

# Archivos binarios (no se tocan los saltos de línea)
*.png  binary
*.jpg  binary
*.ico  binary
*.pdf  binary